{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    // skipLibCheck because invalid .d.ts for miniflare
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts"]
}
