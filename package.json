{
  "name": "@rocicorp/mono",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build-ci": "npm install && turbo run build",
    "start-replicator": "cd packages/zero-cache && npm run start-replicator",
    "start-sync": "cd packages/zero-cache && npm run start-service-runner",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check-format": "turbo run check-format",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@rocicorp/eslint-config": "^0.5.1",
    "@rocicorp/prettier-config": "^0.2.0",
    "prettier": "^3.0.3",
    "syncpack": "^12.3.2",
    "turbo": "^1.13.2",
    "vitest": "^2.0.3"
  },
  "eslintConfig": {
    "extends": "@rocicorp/eslint-config"
  },
  "prettier": "@rocicorp/prettier-config"
}
