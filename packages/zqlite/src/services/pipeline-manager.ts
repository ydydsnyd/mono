/**
 * All the registered IVM pipelines.
 * Rooted at sources.
 *
 * Need row accumulation for pipelines
 * so we can update the CVR.
 *
 * Need retraction accumulations too.
 *
 * A given pipeline needs to know what
 * client groups it is responsible for.
 */
